# Static Assets Directory

Place your generated Android APK file here as `time-out.apk`.

During the build process (`npm run build`), Expo will automatically copy any files in this directory directly into the root of the exported `dist` directory. This allows Vercel to host your APK directly at:
`https://your-domain.vercel.app/time-out.apk`
